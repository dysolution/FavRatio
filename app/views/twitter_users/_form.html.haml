= form_for @twitter_user do |f|
  -if @twitter_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@twitter_user.errors.count, "error")} prohibited this twitter_user from being saved:"
      %ul
        - @twitter_user.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
